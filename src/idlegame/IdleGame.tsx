import { Box, Typography } from "@mui/material";

interface IdleGameProps {
}

function IdleGame(props: IdleGameProps) {
    const { } = props;
    return(
        <Box><Typography variant="h3">Welcome to my idlegame</Typography></Box>
    )
};
export default IdleGame;
